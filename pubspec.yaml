name: app_movil_sist_control
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  mobile_scanner: ^7.0.1
  qr_flutter: ^4.1.0
  http: ^1.1.0
  shared_preferences: ^2.2.3
  flutter_launcher_icons: ^0.14.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/icono_sist_control.png"

flutter:
  uses-material-design: true

  assets:
  - assets/icon/icono_sist_control.png
